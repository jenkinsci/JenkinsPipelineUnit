//Assume these were generated by some helper method that wants to protect the 
//arguments it generates from mutation.
pretendArgsFromFarUpstream = [
    file: "foo.txt",
    text: "All bar, all the time",
].asImmutable()

node() {
    writeFile(pretendArgsFromFarUpstream)
}
