# JenkinsPipelineUnit's own test suite runs in parallel, both to keep the build fast and
# to continuously prove the framework is thread-safe. If this suite ever regresses to
# routing calls across threads, these settings will make it fail.
junit.jupiter.execution.parallel.enabled = true
junit.jupiter.execution.parallel.mode.default = concurrent
junit.jupiter.execution.parallel.mode.classes.default = concurrent
junit.jupiter.execution.parallel.config.strategy = dynamic
junit.jupiter.execution.parallel.config.dynamic.factor = 2
